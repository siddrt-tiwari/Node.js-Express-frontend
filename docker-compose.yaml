version: '3'

services:
  frontend:
    build: ./Frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend

  backend:
    build: ./Backend
    ports:
      - "5001:5000"
    environment:
      - MONGO_URI=mongodb://host.docker.internal:27017
